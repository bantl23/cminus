/* A program to perform Euclid's
   Algorithm to compute god. */

int gcd (int u, int v)
{
  output(u);
  output(v);
  if (v == 0)
  {
    return u;
  }
  else
  {
    return gcd(v,u-u/v*v);
  }
}

void main(void)
{
  int x;
  int y;
  x = input();
  y = input();
  output(x);
  output(y);
  output(gcd(x,y));
}
